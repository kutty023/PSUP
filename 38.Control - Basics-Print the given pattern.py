# Print the given pattern 

n = int(input())  # Read an integer input from the user and store it in n

for i in range(0, n):  # Iterate over numbers from 0 to n-1
    print("* " * n)  # Print a line of n asterisks ('*') separated by a space, repeated n times
